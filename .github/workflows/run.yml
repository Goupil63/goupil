name: Vinted Bot

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */5 * * *"  # toutes les 5 heures à HH:00

jobs:
  run-bot:
    runs-on: ubuntu-latest
    
    concurrency:
      group: vinted-bot
      cancel-in-progress: false  # le nouveau run attend que l'ancien se termine

      
# 1️⃣ Récupération du code
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

        
# 2️⃣ Setup Python
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.x"


# 3️⃣ Installation des dépendances
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4


# 4️⃣ Lancement du script
      - name: Run Vinted Bot
        env:
          VINTED_URL: ${{ secrets.VINTED_URL }}
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
          GH_PAT: ${{ secrets.GH_PAT1 }}
        run: |
          echo "⏳ Lancement du Vinted Bot..."
          end=$((SECONDS+300))  # 5 minutes
          while [ $SECONDS -lt $end ]; do
            echo "📡 Nouvelle analyse à $(date +'%H:%M:%S')..."
            python main.py
            # délai aléatoire entre 180 et 300 secondes
            delay=$((180 + RANDOM % 121))  # 180-300 secondes
            echo "⏱ Prochaine analyse dans $DELAY secondes"
            sleep $DELAY
          done
          echo "✅ Fin du run de 5 minutes"

# 5️⃣ Commit automatique de seen.json
      - name: Commit seen.json
        run: |
          git config --global user.name "Vinted Bot"
          git config --global user.email "vinted-bot@example.com"
          git add seen.json
          git commit -m "Update seen.json [ci skip]" || echo "✅ Pas de nouvelles annonces à commit"
          git push https://x-access-token:${{ secrets.GH_PAT1 }}@github.com/Goupil63/goupil.git HEAD:main
