name: Vinted Bot

on:
  workflow_dispatch:
  schedule:
    - cron: "*/5 * * * *"  # toutes les 5 minutes

jobs:
  run-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 10   # sécurité pour ne pas dépasser 10 minutes
    concurrency:
      group: vinted-bot
      cancel-in-progress: false  # le nouveau run attend que l'ancien se termine

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.x"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4

      - name: Run Vinted Bot
        env:
          VINTED_URL: ${{ secrets.VINTED_URL }}
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
          GH_PAT: ${{ secrets.GH_PAT1 }}
        run: |
          echo "⏳ Lancement du Vinted Bot..."
          end=$((SECONDS+300))  # 5 minutes
          while [ $SECONDS -lt $end ]; do
            echo "📡 Nouvelle analyse à $(date +'%H:%M:%S')..."
            python main.py
            # délai aléatoire entre 180 et 360 secondes
            DELAY=$((RANDOM % 180 + 180))
            echo "⏱ Prochaine analyse dans $DELAY secondes"
            sleep $DELAY
          done
          echo "✅ Fin du run de 5 minutes"

      - name: Commit seen.json
        run: |
          git config --global user.name "Vinted Bot"
          git config --global user.email "vinted-bot@example.com"
          git remote set-url origin https://$GH_PAT1@github.com/Goupil63/goupil.git
          git pull --rebase
          git add seen.json
          git commit -m "Update seen.json [ci skip]" || echo "✅ Pas de nouvelles annonces à commit"
          git push https://x-access-token:${GH_PAT}@github.com/${{ github.repository }}.git HEAD:main
          echo "💾 seen.json mis à jour et poussé sur le repo."
